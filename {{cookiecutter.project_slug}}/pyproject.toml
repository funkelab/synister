[project]
name = "malecns"
description = "Neurotransmitter prediction for the Male CNS of D.melanogaster"
readme = "README.md"
requires-python = ">=3.11,<3.19"
dependencies = [
    "pandas",
    "gunpowder",
    "numpy",
    "tqdm",
    "monai",
    "pyarrow",
    "tensorboardX",
    "funlib.learn.torch @ git+https://github.com/funkelab/funlib.learn.torch.git",
    "hydra-core",
    "omegaconf",
    "scikit-learn",
    "matplotlib",
]
version = "0.1.0"

[tool.pixi.project]
channels = ["https://prefix.dev/conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
torch = { version = "*", index = "https://download.pytorch.org/whl/cu118" }
torchvision = { version = "*", index = "https://download.pytorch.org/whl/cu118" }

[tool.pixi.environments]
default = { solve-group = "default" }

[tool.pixi.tasks]
